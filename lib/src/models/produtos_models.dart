class Produtos {
  final String itemName, itemDesc;
  bool inStorage;
  final double valor;

  Produtos({
    required this.itemName,
    required this.itemDesc,
    required this.inStorage,
    required this.valor,
  });
}
